-- updates the score of Bob to 10 in the table second_table
-- use the name and not Bob's id
-- database will be passed as an argument of the mysql command

UPDATE second_table SET score = 10 WHERE name = "Bob";

